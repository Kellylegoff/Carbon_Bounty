<h2><%= @usecase.name %></h2>
<div>
  <% @usecase.casesiots.each do |result|%>
  <%= name = result.iotproduct.product_name %>
  <%= link_to "Decouvrir la fiche produit", usecase_iotproduct_path(@usecase, result.iotproduct.id) %>
  <% end %>
  <%= link_to "Choisir cette solution", new_customer_path(@usecase) %>
</div>
